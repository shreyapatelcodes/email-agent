[project]
name = "email-agent"
version = "0.1.0"
description = "An MCP server that turns Claude Desktop into your Gmail assistant"
requires-python = ">=3.10"
dependencies = [
    "google-auth>=2.0",
    "google-auth-oauthlib>=1.0",
    "google-api-python-client>=2.0",
    "mcp[cli]>=1.0",
    "pyyaml>=6.0",
]
